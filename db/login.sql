CREATE DATABASE login;

USE login;

CREATE TABLE employees(
				id_user INT AUTO_INCREMENT,

				user_fullname VARCHAR(50),
				user_email VARCHAR(50),
				user_passworld VARCHAR(50),
				user_email VARCHAR(50),
				user_email VARCHAR(50),


				created_date DATETIME,
				updated_date DATETIME,

				PRIMARY KEY(id_user)

					);
